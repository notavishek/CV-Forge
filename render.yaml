services:
  - type: web
    name: cv-forge
    runtime: python
    buildCommand: "pip install --upgrade pip"
    startCommand: "python server.py"
    envVars:
      - key: PORT
        generateValue: false
      - key: GEMINI_API_KEY
        sync: false
    healthCheckPath: /
